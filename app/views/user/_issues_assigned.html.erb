<h3><%= l(:label_assigned_issues) %></h3>

<%= render :partial => 'issues/list_simple', :locals => { :issues => issues } %>

<% if issues.length > 0 %>
	<p class="small">
		<%= link_to l(:label_issue_view_all), :controller => 'issues', :action => 'index', :set_filter => 1, :assigned_to_id => user.id, :sort => 'priority:desc,updated_on:desc' %>
	</p>
<% end %>